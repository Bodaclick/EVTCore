{% spaceless %}
{
    "items": [
        {% for lead in leads.getItems %}
        {% include 'EVTApiBundle:Lead:lead.html.twig' %}
        {% if not loop.last %},{% endif %}
        {% endfor %}
    ],
    "pagination": {
        "total_pages": {{ leads.totalPages }},
        "current_page": {{ leads.currentPageNumber }},
        "items_per_page":{{ leads.numItemsPerPage }},
        "total_items": {{ leads.totalCount }}
    }
}
{% endspaceless %}