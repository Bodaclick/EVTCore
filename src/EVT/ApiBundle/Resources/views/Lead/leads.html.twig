{% spaceless %}
{
    "items": [
        {% for lead in leads.getItems %}
        {% include 'EVTApiBundle:Lead:lead.html.twig' %}
        {% if not loop.last %},{% endif %}
        {% endfor %}
    ],
    "pagination": {
        "total_pages": {{ leads.getPagination.total_pages }},
        "current_page": {{ leads.getPagination.current_page }},
        "items_per_page":{{ leads.getPagination.items_per_page }},
        "total_items": {{ leads.getPagination.total_items }}
    }
}
{% endspaceless %}